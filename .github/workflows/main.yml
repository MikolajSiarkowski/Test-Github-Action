name: Built and deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Firebase
        uses: /firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKES: ${{ secrets.FIREBASE_TOKEN }}
    
